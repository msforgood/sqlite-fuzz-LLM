{
  "target": {
    "function": "sqlite3VdbeSetNumCols", 
    "fc": "vdbe_002",
    "category": "VDBE",
    "file": "vdbeapi.c",
    "line": 1205
  },
  "struct_spec": {
    "vdbe_stmt": {
      "pVdbe": "Vdbe*",
      "nResColumn": "int",
      "azCol": "char**",
      "aColName": "Mem*"
    },
    "column_config": {
      "numCols": "int",
      "colNames": "char*[]",
      "colTypes": "int[]"
    }
  },
  "validation_spec": {
    "memory_align": 8,
    "column_count": {"min": 0, "max": 32767},
    "name_length": {"min": 0, "max": 1024},
    "column_types": ["INTEGER", "REAL", "TEXT", "BLOB", "NULL"],
    "encoding_support": ["UTF8", "UTF16LE", "UTF16BE"],
    "requires_initialization": true
  },
  "fc_mapping": {
    "source": "vdbeapi.c:1205",
    "rationale": "sqlite3VdbeSetNumCols - Critical VDBE result column configuration function",
    "api_level": "internal"
  },
  "constraints": {
    "max_input_size": 4096,
    "min_input_size": 16,
    "endianness": "LE",
    "sqlite_version": "3.51.0",
    "requires_initialization": true
  },
  "notes": {
    "preconditions": [
      "sqlite3_initialize() called",
      "Valid Vdbe statement object",
      "Column count within reasonable limits",
      "Memory available for column metadata"
    ],
    "side_effects": [
      "Column metadata allocated and initialized",
      "Result set structure configured",
      "Column names and types set",
      "Memory usage increased for metadata"
    ],
    "error_conditions": [
      "SQLITE_NOMEM: Memory allocation failure",
      "SQLITE_MISUSE: Invalid column count",
      "SQLITE_RANGE: Column index out of bounds"
    ]
  }
}