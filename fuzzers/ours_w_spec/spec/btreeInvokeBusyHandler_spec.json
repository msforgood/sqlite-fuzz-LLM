{
  "target": { 
    "function": "btreeInvokeBusyHandler",
    "fc": "btree_004",
    "category": "B-Tree",
    "file": "btree.c",
    "line": 2500
  },
  "struct_spec": {
    "btree_shared": {
      "db": "sqlite3*",
      "busyHandler": "BusyHandler*"
    },
    "busy_handler": {
      "xBusyHandler": "int(*)(void*,int)",
      "pBusyArg": "void*",
      "nBusy": "int"
    },
    "btree_context": {
      "pBt": "BtShared*",
      "timeout": "int",
      "retryCount": "int"
    }
  },
  "validation_spec": {
    "memory_align": 8,
    "timeout_range": {"min": 0, "max": 30000},
    "retry_count": {"min": 0, "max": 100},
    "mutex_required": true,
    "busy_handler_validation": true
  },
  "fc_mapping": {
    "source": "btree.c:2500",
    "rationale": "btreeInvokeBusyHandler - Critical function for handling database busy scenarios",
    "api_level": "internal"
  },
  "constraints": {
    "max_input_size": 2048,
    "min_input_size": 16,
    "endianness": "LE",
    "sqlite_version": "3.51.0",
    "requires_initialization": true
  },
  "notes": {
    "preconditions": [
      "sqlite3_initialize() called",
      "Valid BtShared object with mutex held",
      "Valid database connection",
      "Busy handler properly configured"
    ],
    "side_effects": [
      "Calls sqlite3InvokeBusyHandler()",
      "May trigger user-defined busy callback",
      "Can affect transaction timing"
    ],
    "error_conditions": [
      "NULL BtShared pointer",
      "Invalid database connection",
      "Mutex not held",
      "Busy handler not configured"
    ]
  }
}