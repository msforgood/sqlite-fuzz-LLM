{
  "target": { 
    "function": "sqlite3ExpirePreparedStatements",
    "fc": "vdbe_015",
    "category": "VDBE",
    "file": "vdbeaux.c",
    "line": 5333
  },
  "struct_spec": {
    "sqlite3_db": {
      "pVdbe": "Vdbe*",
      "nVdbeActive": "int",
      "nVdbeRead": "int",
      "nVdbeWrite": "int",
      "nVdbeExec": "int",
      "nVTrans": "int",
      "aDb": "Db*",
      "nDb": "int"
    },
    "vdbe": {
      "db": "sqlite3*",
      "pVNext": "Vdbe*",
      "pVPrev": "Vdbe*",
      "expired": "u32",
      "nOp": "int",
      "nOpAlloc": "int",
      "aOp": "VdbeOp*",
      "nLabel": "int",
      "prepFlags": "u8"
    },
    "expire_params": {
      "iCode": "int",
      "expireCount": "u32"
    }
  },
  "validation_spec": {
    "memory_align": 8,
    "expire_code": {"min": 0, "max": 2147483647},
    "vdbe_chain_length": {"min": 0, "max": 1000},
    "db_state": ["SQLITE_STATE_OPEN", "SQLITE_STATE_BUSY"],
    "null_pointer_check": true,
    "chain_integrity": true
  },
  "fc_mapping": {
    "source": "vdbeaux.c:5333",
    "rationale": "sqlite3ExpirePreparedStatements - Critical VDBE statement expiration function",
    "api_level": "public"
  },
  "constraints": {
    "max_input_size": 2048,
    "min_input_size": 12,
    "endianness": "LE",
    "sqlite_version": "3.51.0",
    "requires_initialization": true
  },
  "notes": {
    "preconditions": [
      "sqlite3_initialize() called",
      "Valid sqlite3 database object",
      "VDBE chain properly linked",
      "Database not in corrupted state"
    ],
    "side_effects": [
      "All prepared statements marked as expired",
      "Statement cache invalidated",
      "Next execution will recompile statements",
      "VDBE chain traversed"
    ],
    "error_conditions": [
      "Null database pointer",
      "Corrupted VDBE chain",
      "Invalid expire code"
    ]
  }
}